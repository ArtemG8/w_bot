# 💻 YourProductivity Bot — умный Telegram-бот для отслеживания продуктивности

Современный телеграм-бот для личной продуктивности: отслеживайте время в состоянии потока, записывайте спринты, анализируйте статистику и получайте мотивацию для достижения целей.

## Почему это полезно

- **Таймер потока**: автоматический отсчёт времени концентрации с возможностью паузы и возобновления
- **Гибкая запись**: фиксируйте как автоматические сессии потока, так и спринты вручную
- **Детальная статистика**: просмотр продуктивности по дням (последние 2 недели) и по месяцам с удобной навигацией
- **Мотивация**: вдохновляющие сообщения для поддержания фокуса и дисциплины
- **Простота использования**: интуитивный интерфейс прямо в Telegram

## Основной функционал

### Таймер состояния потока ("/record_flow")
- Запуск таймера для отслеживания времени концентрации
- Пауза и возобновление работы таймера
- Автоматический подсчёт накопленного времени
- Мотивационные сообщения в зависимости от длительности сессии

### Запись спринтов ("/record_sprint")
- Ручной ввод длительности спринта в минутах
- Быстрая фиксация продуктивных интервалов

### История продуктивности ("/history")
- **По дням**: статистика за последние 2 недели с возможностью листать периоды
- **По месяцам**: обзор продуктивности по всем месяцам с данными
- Итоговая статистика за выбранный период
- Удобная навигация между режимами просмотра

### Мотивация ("/motivate")
- Случайные мотивационные сообщения из коллекции
- Поддержка фокуса и дисциплины

## Команды бота

- "/start" — приветствие и список доступных команд
- "/record_flow" — запустить таймер состояния потока
- "/record_sprint" — записать время спринта вручную
- "/history" — посмотреть историю продуктивности (по дням/месяцам)
- "/motivate" — получить мотивационное сообщение

## Технический стек

- **Python 3.12+** — современная версия языка
- **Aiogram v3.22.0** — асинхронный фреймворк для Telegram-ботов
- **PostgreSQL** — надёжная реляционная БД для хранения данных
- **SQLAlchemy 2.0** — современный ORM с асинхронной поддержкой
- **AsyncPG** — быстрый асинхронный драйвер для PostgreSQL
- **python-dotenv** — управление переменными окружения

## Структура проекта

"""
YourProductivity_bot/
├── main.py                 # Точка входа, запуск бота
├── config/
│   └── config.py          # Настройки и конфигурация
├── database/
│   ├── connection.py      # Подключение к БД (PostgreSQL)
│   ├── models.py          # SQLAlchemy модели
│   └── db.py              # Функции работы с БД
├── handlers/
│   └── private_user.py    # Обработчики команд и callback-ов
├── keyboards/
│   ├── flow_kb.py         # Клавиатуры для таймера потока
│   ├── keyboard_utils.py  # Вспомогательные функции клавиатур
│   └── set_menu.py        # Настройка меню бота
├── lexicon/
│   └── lexicon_ru.py      # Тексты и сообщения бота
├── middlewares/
│   └── database.py        # Middleware для работы с БД
├── states/
│   └── states.py          # Состояния FSM для диалогов
├── requirements.txt       # Зависимости проекта
├── docker-compose.yml     # Конфигурация Docker Compose
└── Dockerfile             # Образ Docker
"""


## Установка и запуск

### 1. Клонировать репозиторий

"""bash
git clone <repository_url>
cd YourProductivity_bot
"""

### 2. Создать и активировать виртуальное окружение

**Windows PowerShell:**
"""powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
"""

**Linux/macOS:**
"""bash
python -m venv venv
source venv/bin/activate
"""

### 3. Установить зависимости

"""bash
pip install -U pip wheel
pip install -r requirements.txt
"""

